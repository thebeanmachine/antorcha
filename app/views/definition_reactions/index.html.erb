<h1><strong><%=h @definition.title %></strong> reacties</h1>

<p class="intro">
  Definitie <%= link_to_definition(@definition) %>.
  <%= link_to 'Nieuwe reactie', new_definition_reaction_path(@definition) %> aanmaken.
</p>

<ul>
  <% @reactions.each do |reaction| %>
    <li>
      <% link_to edit_reaction_path(reaction) do %>
        <%=h reaction.cause.title %> &rarr; <%=h reaction.effect.title %>
      <% end %>
    </li>
  <% end%>
</ul>


