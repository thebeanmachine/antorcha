<h1><%=h @message.title %></h1>

<p>Transactiestap <%= @message.step.title %></p>
<p>Verstuurd op <%= @message.delivered? ? l(@message.delivered_at) : 'Niet verstuurd' %>

<p>
  <b>Body:</b>
  <%=h @message.body %>
</p>

<p>
  <b>Incoming?:</b>
  <%=h @message.incoming? %>
</p>

<% unless @message.delivered? %>
  <%= button_to 'Verstuur', message_delivery_path(@message), :method => :post %> |
<% end %>
<%= link_to 'Edit', edit_message_path(@message) %> |
<%= link_to 'Back', messages_path %>