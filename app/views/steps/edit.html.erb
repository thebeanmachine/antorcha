<h1><strong><%=h @step.title %></strong> bewerken</h1>
<p class="intro">binnen transactie <%= link_to h(@task.title), @task %> </p>

<%= render :partial => 'form' %>

<p><%= link_to 'Show', @step %></p>