<h2>Bewerk <%= resource_name.to_s.humanize %></h2>

<% form_for resource_name, resource, :url => registration_path(resource_name), :html => { :method => :put } do |f| -%>
  <%= f.error_messages %>

  <p><%= f.label :email, "E-mail" %></p>
  <p><%= f.text_field :email %></p>

  <p><%= f.label :password, "Wachtwoord" %> <i>(laat deze leeg wanneer u deze niet wilt wijzigen)</i></p>
  <p><%= f.password_field :password %></p>

  <p><%= f.label :password_confirmation, "Wachtwoord herhaling" %></p>
  <p><%= f.password_field :password_confirmation %></p>

  <p><%= f.label :current_password, "Huidige wachtwoord" %> <i>(alleen nodig wanneer u uw wachtwoord wilt wijzigen)</i></p>
  <p><%= f.password_field :current_password %></p>

  <p><%= f.submit "Bijwerken" %></p>
<% end -%>

<h3>Account verwijderen</h3>

<p>Wilt u niet langer in dit systeem? <%= link_to "Verwijder mijn account", registration_path(resource_name), :confirm => "Weet u het zeker?", :method => :delete %>.</p>

<%= render :partial => "shared/devise_links" %>
